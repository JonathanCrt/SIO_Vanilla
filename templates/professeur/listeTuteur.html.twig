{% extends 'base.html.twig' %}

{% block title %} {% endblock %}



{% block body %}

    <link rel="stylesheet" href="{{ asset('assets/css/ListCompany.css') }}">

    <div class="tm-header">
        <div class="container-fluid">
            <div class="tm-header-inner">
                <a href="/" class="navbar-brand tm-site-name titleApp">Vanilla</a>

                <!-- navbar -->
                <nav class="navbar tm-main-nav">

                    <button class="navbar-toggler hidden-md-up" type="button" data-toggle="collapse"
                            data-target="#tmNavbar">
                        &#9776;
                    </button>

                    <div class="collapse navbar-toggleable-sm" id="tmNavbar">
                        <ul class="nav navbar-nav">
                            <li class="nav-item ">
                                <a href="{{ path('indexProfesseur') }}" class="nav-link">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('listeEntrepriseProfesseur') }}" class="nav-link">Liste des entreprises</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('listeUser') }}" class="nav-link">Liste des eleve</a>
                            </li>
                            <li class="nav-item">
                                <a href="/aide" class="nav-link">Aide</a>
                            </li>
                        </ul>
                    </div>

                </nav>



            </div>
        </div>
    </div>
    </br>
    <div class="container">
        <table class="table table-hover table-warning">
            <thead>
            <tr>
                <th>nom Tuteur</th>
                <th>prenom Tuteur</th>
                <th>Code mailTuteur</th>
                <th>telTuteur</th>
                <th>Entreprise</th>
                <th>Liste Stagiaire</th>



            </tr>
            </thead>
            <tbody>
            {% for i in listeTuteurByEntreprise %}

                <tr>
                    <td>{{  i.nomTuteur }}</td>
                    <td>{{  i.prenomTuteur }}</td>
                    <td>{{  i.mailTuteur }}</td>
                    <td>{{  i.telTuteur }}</td>
                    <td>{{  i.entreprise.nomEntreprise }}</td>
                    <td><a href="{{ path('listeStagiaireByTuteur', {'id': i.id}) }}"><span class="icon fa fa-users"></span></a></td>


                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
